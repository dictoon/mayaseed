
cmake_minimum_required (VERSION 2.6)

set (plugin_name "ms_export_obj")

project(${plugin_name})

set (maya_dir "/Applications/Autodesk/maya2012/")
	
include_directories(${maya_dir} "devkit/include")
include_directories(${maya_dir} "devkit/include/maya")

link_directories(${maya_dir} "Maya.app/Contents/MacOS")

add_definitions(-DMAC_PLUGIN -D_BOOL -DREQUIRE_IOSTREAM)

add_library(${plugin_name} SHARED ms_export_obj.cpp)

target_link_libraries(${plugin_name} Foundation OpenMaya)

